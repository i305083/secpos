_schema-version: "3.2"
ID: zsec_pos
description: Position
version: 0.0.1
modules:
- name: zsec_pos-destination-content
  type: com.sap.application.content
  requires:
  - name: zsec_pos-destination-service
    parameters:
      content-target: true
  - name: zsec_pos-repo-host
    parameters:
      service-key:
        name: zsec_pos-repo-host-key
  - name: zsec_pos-uaa
    parameters:
      service-key:
        name: zsec_pos-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zsec_pos_html_repo_host
          ServiceInstanceName: zsec_pos-html5-service
          ServiceKeyName: zsec_pos-repo-host-key
          sap.cloud.service: zsec_pos
        - Authentication: OAuth2UserTokenExchange
          Name: zsec_pos_uaa
          ServiceInstanceName: zsec_pos-xsuaa-service
          ServiceKeyName: zsec_pos-uaa-key
          sap.cloud.service: zsec_pos
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: zsec_pos-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zsec_pos-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zsecpos.zip
      name: zsecpos
      target-path: resources/
- name: zsecpos
  type: html5
  path: zsecpos
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zsec_pos-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zsec_pos-destination-service
    service-plan: lite
- name: zsec_pos-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: zsec_pos-xsuaa-service
    service-plan: application
- name: zsec_pos-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zsec_pos-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
